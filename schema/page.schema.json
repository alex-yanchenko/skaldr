{
  "$defs": {
    "Badge": {
      "additionalProperties": false,
      "properties": {
        "label": {
          "description": "Chip text for this tag/status.",
          "title": "Label",
          "type": "string"
        },
        "tone": {
          "description": "Chip colour \u2014 a palette name (slate/blue/\u2026) or its semantic tone twin (neutral/info/\u2026).",
          "enum": [
            "slate",
            "blue",
            "green",
            "amber",
            "red",
            "violet",
            "teal",
            "sky"
          ],
          "title": "Tone",
          "type": "string"
        },
        "legend": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "const": false,
              "type": "boolean"
            }
          ],
          "description": "One-line meaning, shown in the derived legend. Set `false` to keep this badge out of the legend entirely \u2014 for a one-off inline chip that needs no explanation.",
          "title": "Legend"
        }
      },
      "required": [
        "label",
        "tone",
        "legend"
      ],
      "title": "Badge",
      "type": "object"
    },
    "BadgeGroup": {
      "additionalProperties": false,
      "properties": {
        "label": {
          "description": "Group label, shown in the row's gutter.",
          "minLength": 1,
          "title": "Label",
          "type": "string"
        },
        "items": {
          "description": "Chips in this group: page-vocabulary refs or one-off label+tone pairs.",
          "items": {
            "anyOf": [
              {
                "$ref": "#/$defs/BadgeRef"
              },
              {
                "$ref": "#/$defs/BadgeLiteral"
              }
            ]
          },
          "minItems": 1,
          "title": "Items",
          "type": "array"
        }
      },
      "required": [
        "label",
        "items"
      ],
      "title": "BadgeGroup",
      "type": "object"
    },
    "BadgeLiteral": {
      "additionalProperties": false,
      "properties": {
        "label": {
          "description": "Chip text for a one-off badge (not from the page vocabulary).",
          "title": "Label",
          "type": "string"
        },
        "tone": {
          "description": "Chip colour \u2014 a palette name (slate/blue/\u2026) or its semantic tone twin.",
          "enum": [
            "slate",
            "blue",
            "green",
            "amber",
            "red",
            "violet",
            "teal",
            "sky"
          ],
          "title": "Tone",
          "type": "string"
        }
      },
      "required": [
        "label",
        "tone"
      ],
      "title": "BadgeLiteral",
      "type": "object"
    },
    "BadgeRef": {
      "additionalProperties": false,
      "properties": {
        "key": {
          "description": "A key declared in the page `badges`.",
          "title": "Key",
          "type": "string"
        }
      },
      "required": [
        "key"
      ],
      "title": "BadgeRef",
      "type": "object"
    },
    "BadgeRow": {
      "additionalProperties": false,
      "properties": {
        "span": {
          "anyOf": [
            {
              "ge": 1,
              "le": 6,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Block width in content columns (1 to 6); omit for full width. Same column-count vocabulary as a grid cell's span, relative to the block's container (the page, or the enclosing grid cell when nested). Width only: blocks still stack vertically; use a `grid` to put several in one row.",
          "title": "Span"
        },
        "type": {
          "const": "badge_row",
          "title": "Type",
          "type": "string"
        },
        "label": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional leading label (e.g. 'Affects:') for a flat `items` row.",
          "title": "Label"
        },
        "items": {
          "description": "A flat row of chips: page-vocabulary refs or one-off label+tone pairs. Use this OR `groups`, not both.",
          "items": {
            "anyOf": [
              {
                "$ref": "#/$defs/BadgeRef"
              },
              {
                "$ref": "#/$defs/BadgeLiteral"
              }
            ]
          },
          "title": "Items",
          "type": "array"
        },
        "groups": {
          "description": "Grouped chips \u2014 each group renders as a labelled gutter row. Use this OR `items`, not both.",
          "items": {
            "$ref": "#/$defs/BadgeGroup"
          },
          "title": "Groups",
          "type": "array"
        }
      },
      "required": [
        "type"
      ],
      "title": "BadgeRow",
      "type": "object"
    },
    "Callout": {
      "additionalProperties": false,
      "properties": {
        "span": {
          "anyOf": [
            {
              "ge": 1,
              "le": 6,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Block width in content columns (1 to 6); omit for full width. Same column-count vocabulary as a grid cell's span, relative to the block's container (the page, or the enclosing grid cell when nested). Width only: blocks still stack vertically; use a `grid` to put several in one row.",
          "title": "Span"
        },
        "type": {
          "const": "callout",
          "title": "Type",
          "type": "string"
        },
        "tone": {
          "description": "Accent + tint: info/success/warning/danger (blue/green/amber/red alias in).",
          "enum": [
            "info",
            "success",
            "warning",
            "danger"
          ],
          "title": "Tone",
          "type": "string"
        },
        "title": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional bold title line in the tone colour.",
          "title": "Title"
        },
        "body": {
          "description": "Rich-text body.",
          "title": "Body",
          "type": "string"
        }
      },
      "required": [
        "type",
        "tone",
        "body"
      ],
      "title": "Callout",
      "type": "object"
    },
    "Card": {
      "additionalProperties": false,
      "properties": {
        "label": {
          "description": "Card label above the number.",
          "title": "Label",
          "type": "string"
        },
        "value": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "number"
            },
            {
              "type": "string"
            }
          ],
          "description": "Headline number, or a short status string.",
          "title": "Value"
        },
        "of": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Denominator; renders a derived percentage.",
          "title": "Of"
        },
        "tone": {
          "anyOf": [
            {
              "enum": [
                "neutral",
                "info",
                "success",
                "warning",
                "danger",
                "accent",
                "teal",
                "sky"
              ],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional tone for the top-border accent.",
          "title": "Tone"
        },
        "delta": {
          "anyOf": [
            {
              "$ref": "#/$defs/CardDelta"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional trend chip beside the value (a period-over-period change)."
        },
        "note": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional small caption line under the number.",
          "title": "Note"
        },
        "badges": {
          "description": "Declared badge keys (from the page `badges`) to chip onto this card.",
          "items": {
            "type": "string"
          },
          "title": "Badges",
          "type": "array"
        }
      },
      "required": [
        "label",
        "value"
      ],
      "title": "Card",
      "type": "object"
    },
    "CardDelta": {
      "additionalProperties": false,
      "properties": {
        "label": {
          "description": "Delta text shown beside the value, e.g. '+12%' or '0.3s'.",
          "minLength": 1,
          "title": "Label",
          "type": "string"
        },
        "direction": {
          "anyOf": [
            {
              "enum": [
                "up",
                "down",
                "flat"
              ],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional glyph before the label: \u25b2 up, \u25bc down, \u2192 flat.",
          "title": "Direction"
        },
        "tone": {
          "anyOf": [
            {
              "enum": [
                "neutral",
                "info",
                "success",
                "warning",
                "danger",
                "accent",
                "teal",
                "sky"
              ],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Delta colour \u2014 YOU set it: up isn't always good (down is good for cost/errors), so skaldr never infers it. Omit for a neutral chip.",
          "title": "Tone"
        }
      },
      "required": [
        "label"
      ],
      "title": "CardDelta",
      "type": "object"
    },
    "Cards": {
      "additionalProperties": false,
      "properties": {
        "span": {
          "anyOf": [
            {
              "ge": 1,
              "le": 6,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Block width in content columns (1 to 6); omit for full width. Same column-count vocabulary as a grid cell's span, relative to the block's container (the page, or the enclosing grid cell when nested). Width only: blocks still stack vertically; use a `grid` to put several in one row.",
          "title": "Span"
        },
        "type": {
          "const": "cards",
          "title": "Type",
          "type": "string"
        },
        "items": {
          "description": "Headline-number cards, laid out full-width.",
          "items": {
            "$ref": "#/$defs/Card"
          },
          "minItems": 1,
          "title": "Items",
          "type": "array"
        }
      },
      "required": [
        "type",
        "items"
      ],
      "title": "Cards",
      "type": "object"
    },
    "Chart": {
      "additionalProperties": false,
      "properties": {
        "span": {
          "anyOf": [
            {
              "ge": 1,
              "le": 6,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Block width in content columns (1 to 6); omit for full width. Same column-count vocabulary as a grid cell's span, relative to the block's container (the page, or the enclosing grid cell when nested). Width only: blocks still stack vertically; use a `grid` to put several in one row.",
          "title": "Span"
        },
        "type": {
          "const": "chart",
          "title": "Type",
          "type": "string"
        },
        "variant": {
          "description": "bar: compare a value across categories (grouped, or set stacked). line: a trend across an ordered axis (smoothed). donut: parts of a whole. Provide `categories`+`series` for bar/line, or `slices` for donut. Reach for a chart when a number's SHAPE (trend/spread/share) is the message; use `cards` for standalone figures and `meter` for a single ratio.",
          "enum": [
            "bar",
            "line",
            "donut"
          ],
          "title": "Variant",
          "type": "string"
        },
        "title": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional caption shown above the chart.",
          "title": "Title"
        },
        "categories": {
          "description": "x-axis labels, left to right (bar/line only); each series has one value per category.",
          "items": {
            "type": "string"
          },
          "title": "Categories",
          "type": "array"
        },
        "series": {
          "description": "One or more data series (bar/line only). Multiple series group side by side, or stack.",
          "items": {
            "$ref": "#/$defs/ChartSeries"
          },
          "title": "Series",
          "type": "array"
        },
        "slices": {
          "description": "Donut segments (donut only); shares are derived from the total.",
          "items": {
            "$ref": "#/$defs/ChartSlice"
          },
          "title": "Slices",
          "type": "array"
        },
        "stacked": {
          "default": false,
          "description": "Stack the bar series into one bar per category instead of grouping (bar only).",
          "title": "Stacked",
          "type": "boolean"
        }
      },
      "required": [
        "type",
        "variant"
      ],
      "title": "Chart",
      "type": "object"
    },
    "ChartSeries": {
      "additionalProperties": false,
      "properties": {
        "label": {
          "description": "Series name \u2014 shown in the legend.",
          "minLength": 1,
          "title": "Label",
          "type": "string"
        },
        "values": {
          "description": "One value per category, in the same order as `categories`.",
          "items": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "number"
              }
            ]
          },
          "minItems": 1,
          "title": "Values",
          "type": "array"
        },
        "tone": {
          "anyOf": [
            {
              "enum": [
                "neutral",
                "info",
                "success",
                "warning",
                "danger",
                "accent",
                "teal",
                "sky"
              ],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional tone for this series' bars/line.",
          "title": "Tone"
        }
      },
      "required": [
        "label",
        "values"
      ],
      "title": "ChartSeries",
      "type": "object"
    },
    "ChartSlice": {
      "additionalProperties": false,
      "properties": {
        "label": {
          "description": "Slice name \u2014 shown in the legend.",
          "minLength": 1,
          "title": "Label",
          "type": "string"
        },
        "value": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "number"
            }
          ],
          "description": "Slice magnitude (> 0); its share of the whole is derived.",
          "title": "Value"
        },
        "tone": {
          "anyOf": [
            {
              "enum": [
                "neutral",
                "info",
                "success",
                "warning",
                "danger",
                "accent",
                "teal",
                "sky"
              ],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional tone for this slice.",
          "title": "Tone"
        }
      },
      "required": [
        "label",
        "value"
      ],
      "title": "ChartSlice",
      "type": "object"
    },
    "Code": {
      "additionalProperties": false,
      "properties": {
        "span": {
          "anyOf": [
            {
              "ge": 1,
              "le": 6,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Block width in content columns (1 to 6); omit for full width. Same column-count vocabulary as a grid cell's span, relative to the block's container (the page, or the enclosing grid cell when nested). Width only: blocks still stack vertically; use a `grid` to put several in one row.",
          "title": "Span"
        },
        "type": {
          "const": "code",
          "title": "Type",
          "type": "string"
        },
        "content": {
          "description": "Code/log/config text; rendered verbatim, no highlighting.",
          "title": "Content",
          "type": "string"
        },
        "label": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional label header above the block.",
          "title": "Label"
        },
        "mode": {
          "default": "plain",
          "description": "plain, or diff (+/- lines tinted success/danger).",
          "enum": [
            "plain",
            "diff"
          ],
          "title": "Mode",
          "type": "string"
        }
      },
      "required": [
        "type",
        "content"
      ],
      "title": "Code",
      "type": "object"
    },
    "Column": {
      "additionalProperties": false,
      "properties": {
        "key": {
          "description": "Row-dict key this column reads.",
          "title": "Key",
          "type": "string"
        },
        "label": {
          "description": "Column header text.",
          "title": "Label",
          "type": "string"
        },
        "kind": {
          "description": "text/rich (first one becomes the title column), number, badge (a coloured chip \u2014 see `placement`), or indicator (a colour-only dot in its own column; the cell value is a tone name).",
          "enum": [
            "text",
            "number",
            "badge",
            "rich",
            "indicator"
          ],
          "title": "Kind",
          "type": "string"
        },
        "placement": {
          "default": "title",
          "description": "For a `badge` column: `title` (default) chips the badge under the row's title and ignores the column `label`; `cell` gives the badge its own labelled column, the cell value a badge key or a list of keys (several chips, wrapping). `cell` on a non-badge column is rejected; the default is a no-op elsewhere.",
          "enum": [
            "title",
            "cell"
          ],
          "title": "Placement",
          "type": "string"
        },
        "pct_of_total": {
          "default": false,
          "description": "Show a derived '% of total' caption (needs a reconcile total).",
          "title": "Pct Of Total",
          "type": "boolean"
        },
        "width": {
          "anyOf": [
            {
              "ge": 1,
              "le": 6,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Proportional width weight (1-6); set it on every in-cell column, or none. A `title`-placement badge column takes no width (it rides under the title).",
          "title": "Width"
        }
      },
      "required": [
        "key",
        "label",
        "kind"
      ],
      "title": "Column",
      "type": "object"
    },
    "Comparison": {
      "additionalProperties": false,
      "properties": {
        "span": {
          "anyOf": [
            {
              "ge": 1,
              "le": 6,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Block width in content columns (1 to 6); omit for full width. Same column-count vocabulary as a grid cell's span, relative to the block's container (the page, or the enclosing grid cell when nested). Width only: blocks still stack vertically; use a `grid` to put several in one row.",
          "title": "Span"
        },
        "type": {
          "const": "comparison",
          "title": "Type",
          "type": "string"
        },
        "options": {
          "description": "The things being compared \u2014 the column headers (2+).",
          "items": {
            "type": "string"
          },
          "minItems": 2,
          "title": "Options",
          "type": "array"
        },
        "rows": {
          "description": "Feature rows; each supplies one value per option.",
          "items": {
            "$ref": "#/$defs/ComparisonRow"
          },
          "minItems": 1,
          "title": "Rows",
          "type": "array"
        },
        "highlight": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "0-based index of the recommended option column to emphasise.",
          "title": "Highlight"
        },
        "polarity": {
          "anyOf": [
            {
              "items": {
                "enum": [
                  "positive",
                  "negative"
                ],
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional per-option polarity, one per option (default all positive). In a 'negative' column a true \u2713 reads as BAD (red) and a false \u2717 as GOOD (green) \u2014 for present-is-bad attributes (e.g. 'leaks disk layout'). Affects only \u2713/\u2717 bool cells; the glyph still marks present/absent.",
          "title": "Polarity"
        }
      },
      "required": [
        "type",
        "options",
        "rows"
      ],
      "title": "Comparison",
      "type": "object"
    },
    "ComparisonCell": {
      "additionalProperties": false,
      "properties": {
        "value": {
          "description": "Cell text (for a \u2713/\u2717 pass a bare true/false instead).",
          "minLength": 1,
          "title": "Value",
          "type": "string"
        },
        "tone": {
          "anyOf": [
            {
              "enum": [
                "neutral",
                "info",
                "success",
                "warning",
                "danger",
                "accent",
                "teal",
                "sky"
              ],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional tone for the text.",
          "title": "Tone"
        }
      },
      "required": [
        "value"
      ],
      "title": "ComparisonCell",
      "type": "object"
    },
    "ComparisonRow": {
      "additionalProperties": false,
      "properties": {
        "feature": {
          "description": "Row label \u2014 the attribute being compared.",
          "minLength": 1,
          "title": "Feature",
          "type": "string"
        },
        "values": {
          "description": "One cell per option, in column order.",
          "items": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "string"
              },
              {
                "$ref": "#/$defs/ComparisonCell"
              }
            ]
          },
          "minItems": 1,
          "title": "Values",
          "type": "array"
        }
      },
      "required": [
        "feature",
        "values"
      ],
      "title": "ComparisonRow",
      "type": "object"
    },
    "DefItem": {
      "additionalProperties": false,
      "properties": {
        "term": {
          "description": "The label/term, rendered prominent (e.g. 'Action').",
          "minLength": 1,
          "title": "Term",
          "type": "string"
        },
        "body": {
          "description": "Rich-text definition; blank lines split paragraphs.",
          "minLength": 1,
          "title": "Body",
          "type": "string"
        }
      },
      "required": [
        "term",
        "body"
      ],
      "title": "DefItem",
      "type": "object"
    },
    "DefList": {
      "additionalProperties": false,
      "properties": {
        "span": {
          "anyOf": [
            {
              "ge": 1,
              "le": 6,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Block width in content columns (1 to 6); omit for full width. Same column-count vocabulary as a grid cell's span, relative to the block's container (the page, or the enclosing grid cell when nested). Width only: blocks still stack vertically; use a `grid` to put several in one row.",
          "title": "Span"
        },
        "type": {
          "const": "def_list",
          "title": "Type",
          "type": "string"
        },
        "items": {
          "description": "Term/definition pairs \u2014 a real labelled list for procedural sub-labels like Action / Expected / Say. Unlike `key_value` (compact muted metadata), the term reads as a prominent label and the body is full rich prose.",
          "items": {
            "$ref": "#/$defs/DefItem"
          },
          "minItems": 1,
          "title": "Items",
          "type": "array"
        }
      },
      "required": [
        "type",
        "items"
      ],
      "title": "DefList",
      "type": "object"
    },
    "Fact": {
      "additionalProperties": false,
      "properties": {
        "label": {
          "description": "Fact label (e.g. 'Source').",
          "title": "Label",
          "type": "string"
        },
        "value": {
          "description": "Fact value (e.g. 'prod').",
          "title": "Value",
          "type": "string"
        }
      },
      "required": [
        "label",
        "value"
      ],
      "title": "Fact",
      "type": "object"
    },
    "FactStrip": {
      "additionalProperties": false,
      "properties": {
        "span": {
          "anyOf": [
            {
              "ge": 1,
              "le": 6,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Block width in content columns (1 to 6); omit for full width. Same column-count vocabulary as a grid cell's span, relative to the block's container (the page, or the enclosing grid cell when nested). Width only: blocks still stack vertically; use a `grid` to put several in one row.",
          "title": "Span"
        },
        "type": {
          "const": "fact_strip",
          "title": "Type",
          "type": "string"
        },
        "facts": {
          "description": "1-8 label/value pairs, one line.",
          "items": {
            "$ref": "#/$defs/Fact"
          },
          "maxItems": 8,
          "minItems": 1,
          "title": "Facts",
          "type": "array"
        }
      },
      "required": [
        "type",
        "facts"
      ],
      "title": "FactStrip",
      "type": "object"
    },
    "Fan": {
      "additionalProperties": false,
      "properties": {
        "span": {
          "anyOf": [
            {
              "ge": 1,
              "le": 6,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Block width in content columns (1 to 6); omit for full width. Same column-count vocabulary as a grid cell's span, relative to the block's container (the page, or the enclosing grid cell when nested). Width only: blocks still stack vertically; use a `grid` to put several in one row.",
          "title": "Span"
        },
        "type": {
          "const": "fan",
          "title": "Type",
          "type": "string"
        },
        "hub": {
          "$ref": "#/$defs/FlowStep",
          "description": "The single node \u2014 the 'one' side (a fan-in's target, a fan-out's source)."
        },
        "spokes": {
          "description": "The 'many' side (2+): the nodes that converge into (in) or diverge from (out) the hub.",
          "items": {
            "$ref": "#/$defs/FlowStep"
          },
          "minItems": 2,
          "title": "Spokes",
          "type": "array"
        },
        "direction": {
          "default": "in",
          "description": "in (default): the spokes converge INTO the hub (N\u21921, e.g. 3 source systems \u2192 1 record). out: the hub diverges to the spokes (1\u2192N, e.g. 1 request \u2192 3 services). Reach for a fan when the shape is one-to-many, not a linear pipeline (use flow for that).",
          "enum": [
            "in",
            "out"
          ],
          "title": "Direction",
          "type": "string"
        }
      },
      "required": [
        "type",
        "hub",
        "spokes"
      ],
      "title": "Fan",
      "type": "object"
    },
    "Flow": {
      "additionalProperties": false,
      "properties": {
        "span": {
          "anyOf": [
            {
              "ge": 1,
              "le": 6,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Block width in content columns (1 to 6); omit for full width. Same column-count vocabulary as a grid cell's span, relative to the block's container (the page, or the enclosing grid cell when nested). Width only: blocks still stack vertically; use a `grid` to put several in one row.",
          "title": "Span"
        },
        "type": {
          "const": "flow",
          "title": "Type",
          "type": "string"
        },
        "steps": {
          "description": "Ordered stages (2+); a flow needs two or more nodes.",
          "items": {
            "$ref": "#/$defs/FlowStep"
          },
          "minItems": 2,
          "title": "Steps",
          "type": "array"
        },
        "style": {
          "default": "arrow",
          "description": "arrow (default): short-labelled nodes joined by \u2192 connectors \u2014 reach for it when the DIRECTION between stages is the message (a pipeline or a data flow). steps: equal cards that each carry a caption line \u2014 reach for it when every stage needs a sentence of explanation.",
          "enum": [
            "arrow",
            "steps"
          ],
          "title": "Style",
          "type": "string"
        },
        "loop": {
          "default": false,
          "description": "Draw a '\u21ba back to <first>' return marker after the last node \u2014 for a cycle, not a one-way pipeline.",
          "title": "Loop",
          "type": "boolean"
        },
        "numbered": {
          "default": true,
          "description": "Number the nodes 1..n (derived); set false to hide numbers.",
          "title": "Numbered",
          "type": "boolean"
        }
      },
      "required": [
        "type",
        "steps"
      ],
      "title": "Flow",
      "type": "object"
    },
    "FlowStep": {
      "additionalProperties": false,
      "properties": {
        "label": {
          "description": "Short stage name \u2014 the node label.",
          "minLength": 1,
          "title": "Label",
          "type": "string"
        },
        "tone": {
          "anyOf": [
            {
              "enum": [
                "neutral",
                "info",
                "success",
                "warning",
                "danger",
                "accent",
                "teal",
                "sky"
              ],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional tone accent for this node's border + number.",
          "title": "Tone"
        },
        "note": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional one-line detail (rich text). Shown as the caption in `steps` style, and as a small sub-line in `arrow` style. If most nodes need a note, prefer style: steps.",
          "title": "Note"
        },
        "points": {
          "description": "Optional detail bullets (rich text) under the node, for when one line isn't enough. Render below the note. Best paired with style: steps \u2014 a few bullets crowd a compact arrow chip.",
          "items": {
            "type": "string"
          },
          "title": "Points",
          "type": "array"
        },
        "badges": {
          "description": "Declared badge keys (from the page `badges`) to chip onto this node.",
          "items": {
            "type": "string"
          },
          "title": "Badges",
          "type": "array"
        }
      },
      "required": [
        "label"
      ],
      "title": "FlowStep",
      "type": "object"
    },
    "Grid": {
      "additionalProperties": false,
      "properties": {
        "span": {
          "anyOf": [
            {
              "ge": 1,
              "le": 6,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Block width in content columns (1 to 6); omit for full width. Same column-count vocabulary as a grid cell's span, relative to the block's container (the page, or the enclosing grid cell when nested). Width only: blocks still stack vertically; use a `grid` to put several in one row.",
          "title": "Span"
        },
        "type": {
          "const": "grid",
          "title": "Type",
          "type": "string"
        },
        "cells": {
          "description": "Cells across a 6-column row.",
          "items": {
            "$ref": "#/$defs/GridCell"
          },
          "minItems": 1,
          "title": "Cells",
          "type": "array"
        }
      },
      "required": [
        "type",
        "cells"
      ],
      "title": "Grid",
      "type": "object"
    },
    "GridCell": {
      "additionalProperties": false,
      "properties": {
        "span": {
          "description": "Columns this cell spans, of 6.",
          "maximum": 6,
          "minimum": 1,
          "title": "Span",
          "type": "integer"
        },
        "blocks": {
          "description": "Blocks stacked in the cell; may include nested grids (depth 2 max).",
          "items": {
            "discriminator": {
              "mapping": {
                "badge_row": "#/$defs/BadgeRow",
                "callout": "#/$defs/Callout",
                "cards": "#/$defs/Cards",
                "chart": "#/$defs/Chart",
                "code": "#/$defs/Code",
                "comparison": "#/$defs/Comparison",
                "def_list": "#/$defs/DefList",
                "fact_strip": "#/$defs/FactStrip",
                "fan": "#/$defs/Fan",
                "flow": "#/$defs/Flow",
                "grid": "#/$defs/InnerGrid",
                "heading": "#/$defs/Heading",
                "image": "#/$defs/Image",
                "key_value": "#/$defs/KeyValue",
                "list": "#/$defs/ListBlock",
                "meter": "#/$defs/Meter",
                "note": "#/$defs/Note",
                "quote": "#/$defs/Quote",
                "range": "#/$defs/Range",
                "references": "#/$defs/References",
                "status_list": "#/$defs/StatusList",
                "swimlane": "#/$defs/Swimlane",
                "table": "#/$defs/Table",
                "text": "#/$defs/Text",
                "timeline": "#/$defs/Timeline"
              },
              "propertyName": "type"
            },
            "oneOf": [
              {
                "$ref": "#/$defs/Heading"
              },
              {
                "$ref": "#/$defs/Text"
              },
              {
                "$ref": "#/$defs/ListBlock"
              },
              {
                "$ref": "#/$defs/FactStrip"
              },
              {
                "$ref": "#/$defs/KeyValue"
              },
              {
                "$ref": "#/$defs/DefList"
              },
              {
                "$ref": "#/$defs/Cards"
              },
              {
                "$ref": "#/$defs/BadgeRow"
              },
              {
                "$ref": "#/$defs/Callout"
              },
              {
                "$ref": "#/$defs/StatusList"
              },
              {
                "$ref": "#/$defs/Meter"
              },
              {
                "$ref": "#/$defs/Range"
              },
              {
                "$ref": "#/$defs/Table"
              },
              {
                "$ref": "#/$defs/Code"
              },
              {
                "$ref": "#/$defs/Quote"
              },
              {
                "$ref": "#/$defs/Note"
              },
              {
                "$ref": "#/$defs/Image"
              },
              {
                "$ref": "#/$defs/Timeline"
              },
              {
                "$ref": "#/$defs/Flow"
              },
              {
                "$ref": "#/$defs/Fan"
              },
              {
                "$ref": "#/$defs/Chart"
              },
              {
                "$ref": "#/$defs/Comparison"
              },
              {
                "$ref": "#/$defs/Swimlane"
              },
              {
                "$ref": "#/$defs/References"
              },
              {
                "$ref": "#/$defs/InnerGrid"
              }
            ]
          },
          "minItems": 1,
          "title": "Blocks",
          "type": "array"
        },
        "tone": {
          "anyOf": [
            {
              "enum": [
                "neutral",
                "info",
                "success",
                "warning",
                "danger",
                "accent",
                "teal",
                "sky"
              ],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional tone: turns the cell into an emphasis panel (accent top-border + tint). Use `neutral` for a muted aside, `accent`/`success` for a primary panel.",
          "title": "Tone"
        }
      },
      "required": [
        "span",
        "blocks"
      ],
      "title": "GridCell",
      "type": "object"
    },
    "Group": {
      "additionalProperties": false,
      "properties": {
        "name": {
          "description": "Group band label; shows the derived subtotal.",
          "title": "Name",
          "type": "string"
        },
        "rows": {
          "description": "Rows in this group \u2014 each a mapping or a positional list in the declared column order. Empty renders a '\u2014 none \u2014' row.",
          "items": {
            "anyOf": [
              {
                "additionalProperties": true,
                "type": "object"
              },
              {
                "items": {},
                "type": "array"
              }
            ]
          },
          "title": "Rows",
          "type": "array"
        }
      },
      "required": [
        "name"
      ],
      "title": "Group",
      "type": "object"
    },
    "Handled": {
      "additionalProperties": false,
      "properties": {
        "label": {
          "description": "Label for the non-issue bucket (e.g. 'Imported cleanly').",
          "title": "Label",
          "type": "string"
        },
        "value": {
          "description": "Count in the bucket; added to the column sum for reconciliation.",
          "title": "Value",
          "type": "integer"
        }
      },
      "required": [
        "label",
        "value"
      ],
      "title": "Handled",
      "type": "object"
    },
    "Heading": {
      "additionalProperties": false,
      "properties": {
        "span": {
          "anyOf": [
            {
              "ge": 1,
              "le": 6,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Block width in content columns (1 to 6); omit for full width. Same column-count vocabulary as a grid cell's span, relative to the block's container (the page, or the enclosing grid cell when nested). Width only: blocks still stack vertically; use a `grid` to put several in one row.",
          "title": "Span"
        },
        "type": {
          "const": "heading",
          "title": "Type",
          "type": "string"
        },
        "text": {
          "description": "Heading text; also the TOC entry at level 2.",
          "minLength": 1,
          "title": "Text",
          "type": "string"
        },
        "level": {
          "default": 2,
          "description": "Heading level: 2 (major heading) or 3 (sub-heading).",
          "enum": [
            2,
            3
          ],
          "title": "Level",
          "type": "integer"
        },
        "id": {
          "anyOf": [
            {
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional stable anchor id (lowercase, hyphen-separated). Overrides the text-derived slug so `[\u2026](#id)` links survive a heading rename. Must be unique across the page.",
          "title": "Id"
        },
        "sub": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional caption line under the heading, styled subordinate \u2014 a real subtitle slot instead of a muted `text` paragraph faking one. Rich text. Does not feed the TOC (that stays the plain `text`).",
          "title": "Sub"
        }
      },
      "required": [
        "type",
        "text"
      ],
      "title": "Heading",
      "type": "object"
    },
    "Image": {
      "additionalProperties": false,
      "properties": {
        "span": {
          "anyOf": [
            {
              "ge": 1,
              "le": 6,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Block width in content columns (1 to 6); omit for full width. Same column-count vocabulary as a grid cell's span, relative to the block's container (the page, or the enclosing grid cell when nested). Width only: blocks still stack vertically; use a `grid` to put several in one row.",
          "title": "Span"
        },
        "type": {
          "const": "image",
          "title": "Type",
          "type": "string"
        },
        "src": {
          "description": "A data: URI (self-contained \u2014 no external fetches). Base64-encode the payload (e.g. data:image/svg+xml;base64,...); a raw, unencoded SVG isn't a valid URI and won't render.",
          "title": "Src",
          "type": "string"
        },
        "alt": {
          "description": "Alt text for the image.",
          "title": "Alt",
          "type": "string"
        },
        "caption": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional caption shown below the image.",
          "title": "Caption"
        },
        "max_width": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional max width in pixels.",
          "title": "Max Width"
        }
      },
      "required": [
        "type",
        "src",
        "alt"
      ],
      "title": "Image",
      "type": "object"
    },
    "InnerGrid": {
      "additionalProperties": false,
      "properties": {
        "span": {
          "anyOf": [
            {
              "ge": 1,
              "le": 6,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Block width in content columns (1 to 6); omit for full width. Same column-count vocabulary as a grid cell's span, relative to the block's container (the page, or the enclosing grid cell when nested). Width only: blocks still stack vertically; use a `grid` to put several in one row.",
          "title": "Span"
        },
        "type": {
          "const": "grid",
          "title": "Type",
          "type": "string"
        },
        "cells": {
          "description": "Cells across the 6-column row.",
          "items": {
            "$ref": "#/$defs/InnerGridCell"
          },
          "minItems": 1,
          "title": "Cells",
          "type": "array"
        }
      },
      "required": [
        "type",
        "cells"
      ],
      "title": "InnerGrid",
      "type": "object"
    },
    "InnerGridCell": {
      "additionalProperties": false,
      "properties": {
        "span": {
          "description": "Columns this cell spans, of 6.",
          "maximum": 6,
          "minimum": 1,
          "title": "Span",
          "type": "integer"
        },
        "blocks": {
          "description": "Leaf blocks stacked in the cell.",
          "items": {
            "discriminator": {
              "mapping": {
                "badge_row": "#/$defs/BadgeRow",
                "callout": "#/$defs/Callout",
                "cards": "#/$defs/Cards",
                "chart": "#/$defs/Chart",
                "code": "#/$defs/Code",
                "comparison": "#/$defs/Comparison",
                "def_list": "#/$defs/DefList",
                "fact_strip": "#/$defs/FactStrip",
                "fan": "#/$defs/Fan",
                "flow": "#/$defs/Flow",
                "heading": "#/$defs/Heading",
                "image": "#/$defs/Image",
                "key_value": "#/$defs/KeyValue",
                "list": "#/$defs/ListBlock",
                "meter": "#/$defs/Meter",
                "note": "#/$defs/Note",
                "quote": "#/$defs/Quote",
                "range": "#/$defs/Range",
                "references": "#/$defs/References",
                "status_list": "#/$defs/StatusList",
                "swimlane": "#/$defs/Swimlane",
                "table": "#/$defs/Table",
                "text": "#/$defs/Text",
                "timeline": "#/$defs/Timeline"
              },
              "propertyName": "type"
            },
            "oneOf": [
              {
                "$ref": "#/$defs/Heading"
              },
              {
                "$ref": "#/$defs/Text"
              },
              {
                "$ref": "#/$defs/ListBlock"
              },
              {
                "$ref": "#/$defs/FactStrip"
              },
              {
                "$ref": "#/$defs/KeyValue"
              },
              {
                "$ref": "#/$defs/DefList"
              },
              {
                "$ref": "#/$defs/Cards"
              },
              {
                "$ref": "#/$defs/BadgeRow"
              },
              {
                "$ref": "#/$defs/Callout"
              },
              {
                "$ref": "#/$defs/StatusList"
              },
              {
                "$ref": "#/$defs/Meter"
              },
              {
                "$ref": "#/$defs/Range"
              },
              {
                "$ref": "#/$defs/Table"
              },
              {
                "$ref": "#/$defs/Code"
              },
              {
                "$ref": "#/$defs/Quote"
              },
              {
                "$ref": "#/$defs/Note"
              },
              {
                "$ref": "#/$defs/Image"
              },
              {
                "$ref": "#/$defs/Timeline"
              },
              {
                "$ref": "#/$defs/Flow"
              },
              {
                "$ref": "#/$defs/Fan"
              },
              {
                "$ref": "#/$defs/Chart"
              },
              {
                "$ref": "#/$defs/Comparison"
              },
              {
                "$ref": "#/$defs/Swimlane"
              },
              {
                "$ref": "#/$defs/References"
              }
            ]
          },
          "minItems": 1,
          "title": "Blocks",
          "type": "array"
        },
        "tone": {
          "anyOf": [
            {
              "enum": [
                "neutral",
                "info",
                "success",
                "warning",
                "danger",
                "accent",
                "teal",
                "sky"
              ],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional tone: turns the cell into an emphasis panel (accent top-border + tint). Use `neutral` for a muted aside, `accent`/`success` for a primary panel.",
          "title": "Tone"
        }
      },
      "required": [
        "span",
        "blocks"
      ],
      "title": "InnerGridCell",
      "type": "object"
    },
    "KVPair": {
      "additionalProperties": false,
      "properties": {
        "label": {
          "description": "Row label (muted).",
          "title": "Label",
          "type": "string"
        },
        "value": {
          "description": "Rich-text value.",
          "title": "Value",
          "type": "string"
        }
      },
      "required": [
        "label",
        "value"
      ],
      "title": "KVPair",
      "type": "object"
    },
    "KeyValue": {
      "additionalProperties": false,
      "properties": {
        "span": {
          "anyOf": [
            {
              "ge": 1,
              "le": 6,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Block width in content columns (1 to 6); omit for full width. Same column-count vocabulary as a grid cell's span, relative to the block's container (the page, or the enclosing grid cell when nested). Width only: blocks still stack vertically; use a `grid` to put several in one row.",
          "title": "Span"
        },
        "type": {
          "const": "key_value",
          "title": "Type",
          "type": "string"
        },
        "pairs": {
          "description": "Vertical label/value metadata rows.",
          "items": {
            "$ref": "#/$defs/KVPair"
          },
          "minItems": 1,
          "title": "Pairs",
          "type": "array"
        }
      },
      "required": [
        "type",
        "pairs"
      ],
      "title": "KeyValue",
      "type": "object"
    },
    "ListBlock": {
      "additionalProperties": false,
      "properties": {
        "span": {
          "anyOf": [
            {
              "ge": 1,
              "le": 6,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Block width in content columns (1 to 6); omit for full width. Same column-count vocabulary as a grid cell's span, relative to the block's container (the page, or the enclosing grid cell when nested). Width only: blocks still stack vertically; use a `grid` to put several in one row.",
          "title": "Span"
        },
        "type": {
          "const": "list",
          "title": "Type",
          "type": "string"
        },
        "style": {
          "default": "bullet",
          "description": "Bulleted, numbered, or `check` \u2014 tickable checkboxes for a live checklist (the ticks are ephemeral: a browser reload resets them).",
          "enum": [
            "bullet",
            "number",
            "check"
          ],
          "title": "Style",
          "type": "string"
        },
        "items": {
          "description": "Rich-text points. A point is a plain string, or `{text, items: [...]}` to nest sub-points (nested lists inherit the parent's style; up to 4 levels deep).",
          "items": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "$ref": "#/$defs/ListItem"
              }
            ]
          },
          "minItems": 1,
          "title": "Items",
          "type": "array"
        }
      },
      "required": [
        "type",
        "items"
      ],
      "title": "ListBlock",
      "type": "object"
    },
    "ListItem": {
      "additionalProperties": false,
      "properties": {
        "text": {
          "description": "The point's rich-text content.",
          "minLength": 1,
          "title": "Text",
          "type": "string"
        },
        "checked": {
          "default": false,
          "description": "Only meaningful in a `style: check` list: renders the box ticked. Set it in the YAML to record durable progress (an agent editing its plan marks items done here); a reader clicking a box in the browser is ephemeral and does not persist.",
          "title": "Checked",
          "type": "boolean"
        },
        "items": {
          "default": [],
          "description": "Optional nested sub-points, rendered as an indented list in the parent's style.",
          "items": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "$ref": "#/$defs/ListItem"
              }
            ]
          },
          "title": "Items",
          "type": "array"
        }
      },
      "required": [
        "text"
      ],
      "title": "ListItem",
      "type": "object"
    },
    "Meta": {
      "additionalProperties": false,
      "properties": {
        "title": {
          "description": "Page title (h1).",
          "title": "Title",
          "type": "string"
        },
        "subtitle": {
          "description": "Subtitle lines under the title.",
          "items": {
            "type": "string"
          },
          "title": "Subtitle",
          "type": "array"
        },
        "source": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Provenance; feeds the footer.",
          "title": "Source"
        },
        "date": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Report date; feeds the footer (never auto-now).",
          "title": "Date"
        },
        "updated": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "When the report was last revised; feeds the footer as 'updated <value>'. A free-form label like the date (author it \u2014 never auto-now).",
          "title": "Updated"
        },
        "toc": {
          "default": false,
          "description": "Render a table of contents from top-level level-2 headings and sections.",
          "title": "Toc",
          "type": "boolean"
        },
        "hero": {
          "default": false,
          "description": "Opt-in hero header: a larger display title + subtitle in a tinted band, for a page that opens by selling an idea rather than a plain report header.",
          "title": "Hero",
          "type": "boolean"
        }
      },
      "required": [
        "title"
      ],
      "title": "Meta",
      "type": "object"
    },
    "Meter": {
      "additionalProperties": false,
      "properties": {
        "span": {
          "anyOf": [
            {
              "ge": 1,
              "le": 6,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Block width in content columns (1 to 6); omit for full width. Same column-count vocabulary as a grid cell's span, relative to the block's container (the page, or the enclosing grid cell when nested). Width only: blocks still stack vertically; use a `grid` to put several in one row.",
          "title": "Span"
        },
        "type": {
          "const": "meter",
          "title": "Type",
          "type": "string"
        },
        "items": {
          "description": "Labelled horizontal bars.",
          "items": {
            "$ref": "#/$defs/MeterItem"
          },
          "minItems": 1,
          "title": "Items",
          "type": "array"
        }
      },
      "required": [
        "type",
        "items"
      ],
      "title": "Meter",
      "type": "object"
    },
    "MeterItem": {
      "additionalProperties": false,
      "properties": {
        "label": {
          "description": "Bar label.",
          "title": "Label",
          "type": "string"
        },
        "value": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "number"
            }
          ],
          "description": "Filled amount (0 \u2264 value \u2264 max).",
          "title": "Value"
        },
        "max": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "number"
            }
          ],
          "description": "Bar maximum (> 0); the denominator for the derived percentage.",
          "title": "Max"
        },
        "tone": {
          "anyOf": [
            {
              "enum": [
                "neutral",
                "info",
                "success",
                "warning",
                "danger",
                "accent",
                "teal",
                "sky"
              ],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional tone for the bar fill.",
          "title": "Tone"
        }
      },
      "required": [
        "label",
        "value",
        "max"
      ],
      "title": "MeterItem",
      "type": "object"
    },
    "Note": {
      "additionalProperties": false,
      "properties": {
        "span": {
          "anyOf": [
            {
              "ge": 1,
              "le": 6,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Block width in content columns (1 to 6); omit for full width. Same column-count vocabulary as a grid cell's span, relative to the block's container (the page, or the enclosing grid cell when nested). Width only: blocks still stack vertically; use a `grid` to put several in one row.",
          "title": "Span"
        },
        "type": {
          "const": "note",
          "title": "Type",
          "type": "string"
        },
        "body": {
          "description": "Rich-text aside; blank lines split paragraphs.",
          "minLength": 1,
          "title": "Body",
          "type": "string"
        },
        "title": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional label for the note.",
          "title": "Title"
        }
      },
      "required": [
        "type",
        "body"
      ],
      "title": "Note",
      "type": "object"
    },
    "Panel": {
      "additionalProperties": false,
      "properties": {
        "span": {
          "anyOf": [
            {
              "ge": 1,
              "le": 6,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Block width in content columns (1 to 6); omit for full width. Same column-count vocabulary as a grid cell's span, relative to the block's container (the page, or the enclosing grid cell when nested). Width only: blocks still stack vertically; use a `grid` to put several in one row.",
          "title": "Span"
        },
        "type": {
          "const": "panel",
          "title": "Type",
          "type": "string"
        },
        "title": {
          "description": "Panel title, shown in the header band.",
          "minLength": 1,
          "title": "Title",
          "type": "string"
        },
        "blocks": {
          "description": "Blocks inside the panel (leaf blocks). Unlike a `section`, a panel is always open \u2014 a titled framed card, one per 'slide' in a deck-style doc.",
          "items": {
            "discriminator": {
              "mapping": {
                "badge_row": "#/$defs/BadgeRow",
                "callout": "#/$defs/Callout",
                "cards": "#/$defs/Cards",
                "chart": "#/$defs/Chart",
                "code": "#/$defs/Code",
                "comparison": "#/$defs/Comparison",
                "def_list": "#/$defs/DefList",
                "fact_strip": "#/$defs/FactStrip",
                "fan": "#/$defs/Fan",
                "flow": "#/$defs/Flow",
                "heading": "#/$defs/Heading",
                "image": "#/$defs/Image",
                "key_value": "#/$defs/KeyValue",
                "list": "#/$defs/ListBlock",
                "meter": "#/$defs/Meter",
                "note": "#/$defs/Note",
                "quote": "#/$defs/Quote",
                "range": "#/$defs/Range",
                "references": "#/$defs/References",
                "status_list": "#/$defs/StatusList",
                "swimlane": "#/$defs/Swimlane",
                "table": "#/$defs/Table",
                "text": "#/$defs/Text",
                "timeline": "#/$defs/Timeline"
              },
              "propertyName": "type"
            },
            "oneOf": [
              {
                "$ref": "#/$defs/Heading"
              },
              {
                "$ref": "#/$defs/Text"
              },
              {
                "$ref": "#/$defs/ListBlock"
              },
              {
                "$ref": "#/$defs/FactStrip"
              },
              {
                "$ref": "#/$defs/KeyValue"
              },
              {
                "$ref": "#/$defs/DefList"
              },
              {
                "$ref": "#/$defs/Cards"
              },
              {
                "$ref": "#/$defs/BadgeRow"
              },
              {
                "$ref": "#/$defs/Callout"
              },
              {
                "$ref": "#/$defs/StatusList"
              },
              {
                "$ref": "#/$defs/Meter"
              },
              {
                "$ref": "#/$defs/Range"
              },
              {
                "$ref": "#/$defs/Table"
              },
              {
                "$ref": "#/$defs/Code"
              },
              {
                "$ref": "#/$defs/Quote"
              },
              {
                "$ref": "#/$defs/Note"
              },
              {
                "$ref": "#/$defs/Image"
              },
              {
                "$ref": "#/$defs/Timeline"
              },
              {
                "$ref": "#/$defs/Flow"
              },
              {
                "$ref": "#/$defs/Fan"
              },
              {
                "$ref": "#/$defs/Chart"
              },
              {
                "$ref": "#/$defs/Comparison"
              },
              {
                "$ref": "#/$defs/Swimlane"
              },
              {
                "$ref": "#/$defs/References"
              }
            ]
          },
          "minItems": 1,
          "title": "Blocks",
          "type": "array"
        }
      },
      "required": [
        "type",
        "title",
        "blocks"
      ],
      "title": "Panel",
      "type": "object"
    },
    "Quote": {
      "additionalProperties": false,
      "properties": {
        "span": {
          "anyOf": [
            {
              "ge": 1,
              "le": 6,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Block width in content columns (1 to 6); omit for full width. Same column-count vocabulary as a grid cell's span, relative to the block's container (the page, or the enclosing grid cell when nested). Width only: blocks still stack vertically; use a `grid` to put several in one row.",
          "title": "Span"
        },
        "type": {
          "const": "quote",
          "title": "Type",
          "type": "string"
        },
        "body": {
          "description": "Rich-text quotation.",
          "title": "Body",
          "type": "string"
        },
        "cite": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional attribution line.",
          "title": "Cite"
        }
      },
      "required": [
        "type",
        "body"
      ],
      "title": "Quote",
      "type": "object"
    },
    "Range": {
      "additionalProperties": false,
      "properties": {
        "span": {
          "anyOf": [
            {
              "ge": 1,
              "le": 6,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Block width in content columns (1 to 6); omit for full width. Same column-count vocabulary as a grid cell's span, relative to the block's container (the page, or the enclosing grid cell when nested). Width only: blocks still stack vertically; use a `grid` to put several in one row.",
          "title": "Span"
        },
        "type": {
          "const": "range",
          "title": "Type",
          "type": "string"
        },
        "segments": {
          "description": "Segments laid left-to-right, each sized in proportion to its span.",
          "items": {
            "$ref": "#/$defs/RangeSegment"
          },
          "minItems": 1,
          "title": "Segments",
          "type": "array"
        },
        "axis": {
          "anyOf": [
            {
              "$ref": "#/$defs/RangeAxis"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional end-cap labels marking the bar's extent."
        }
      },
      "required": [
        "type",
        "segments"
      ],
      "title": "Range",
      "type": "object"
    },
    "RangeAxis": {
      "additionalProperties": false,
      "properties": {
        "min": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Label at the left end of the bar (e.g. a start year).",
          "title": "Min"
        },
        "max": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Label at the right end of the bar (e.g. an end year).",
          "title": "Max"
        }
      },
      "title": "RangeAxis",
      "type": "object"
    },
    "RangeSegment": {
      "additionalProperties": false,
      "properties": {
        "label": {
          "description": "Label shown inside the segment.",
          "minLength": 1,
          "title": "Label",
          "type": "string"
        },
        "span": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "number"
            }
          ],
          "description": "Relative width (> 0). Spans are normalised across the segments, so only the ratios matter \u2014 [3, 1] and [30, 10] render identically.",
          "title": "Span"
        },
        "tone": {
          "anyOf": [
            {
              "enum": [
                "neutral",
                "info",
                "success",
                "warning",
                "danger",
                "accent",
                "teal",
                "sky"
              ],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Soft-tint fill + text colour for the segment (defaults to neutral).",
          "title": "Tone"
        },
        "sub": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional rich-text sub-line under the label.",
          "title": "Sub"
        }
      },
      "required": [
        "label",
        "span"
      ],
      "title": "RangeSegment",
      "type": "object"
    },
    "Reconcile": {
      "additionalProperties": false,
      "properties": {
        "total": {
          "description": "Denominator; the column sum + handled must equal this.",
          "exclusiveMinimum": 0,
          "title": "Total",
          "type": "integer"
        },
        "column": {
          "description": "Key of the number column that must sum to `total`.",
          "title": "Column",
          "type": "string"
        },
        "handled": {
          "anyOf": [
            {
              "$ref": "#/$defs/Handled"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "A bucket outside the rows."
        }
      },
      "required": [
        "total",
        "column"
      ],
      "title": "Reconcile",
      "type": "object"
    },
    "ReferenceItem": {
      "additionalProperties": false,
      "properties": {
        "key": {
          "description": "Short id (ASCII letters, digits, _, -); cite it inline with [^key].",
          "minLength": 1,
          "pattern": "^[A-Za-z0-9_-]+$",
          "title": "Key",
          "type": "string"
        },
        "text": {
          "description": "Rich-text source description (e.g. a doc name + page).",
          "minLength": 1,
          "title": "Text",
          "type": "string"
        },
        "url": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional link for the source (http/https/mailto).",
          "title": "Url"
        }
      },
      "required": [
        "key",
        "text"
      ],
      "title": "ReferenceItem",
      "type": "object"
    },
    "References": {
      "additionalProperties": false,
      "properties": {
        "span": {
          "anyOf": [
            {
              "ge": 1,
              "le": 6,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Block width in content columns (1 to 6); omit for full width. Same column-count vocabulary as a grid cell's span, relative to the block's container (the page, or the enclosing grid cell when nested). Width only: blocks still stack vertically; use a `grid` to put several in one row.",
          "title": "Span"
        },
        "type": {
          "const": "references",
          "title": "Type",
          "type": "string"
        },
        "items": {
          "description": "Numbered sources; cite each inline with [^key].",
          "items": {
            "$ref": "#/$defs/ReferenceItem"
          },
          "minItems": 1,
          "title": "Items",
          "type": "array"
        }
      },
      "required": [
        "type",
        "items"
      ],
      "title": "References",
      "type": "object"
    },
    "Rollup": {
      "additionalProperties": false,
      "properties": {
        "by": {
          "description": "Key of the badge column whose per-row values are counted.",
          "title": "By",
          "type": "string"
        },
        "label": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional label shown before the counted chips.",
          "title": "Label"
        }
      },
      "required": [
        "by"
      ],
      "title": "Rollup",
      "type": "object"
    },
    "Section": {
      "additionalProperties": false,
      "properties": {
        "span": {
          "anyOf": [
            {
              "ge": 1,
              "le": 6,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Block width in content columns (1 to 6); omit for full width. Same column-count vocabulary as a grid cell's span, relative to the block's container (the page, or the enclosing grid cell when nested). Width only: blocks still stack vertically; use a `grid` to put several in one row.",
          "title": "Span"
        },
        "type": {
          "const": "section",
          "title": "Type",
          "type": "string"
        },
        "title": {
          "description": "Summary label shown on the collapsible.",
          "title": "Title",
          "type": "string"
        },
        "id": {
          "anyOf": [
            {
              "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional stable anchor id (lowercase, hyphen-separated). Overrides the title-derived slug so `[\u2026](#id)` links survive a title rename. Must be unique across the page.",
          "title": "Id"
        },
        "collapsed": {
          "default": true,
          "description": "Whether the section starts collapsed. Default true suits an appendix / detail; set false for a read-through living doc so the section opens expanded.",
          "title": "Collapsed",
          "type": "boolean"
        },
        "updated": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "When this section was last revised; shown as a muted stamp in its header. A free-form label like the report date (author it \u2014 never auto-now).",
          "title": "Updated"
        },
        "blocks": {
          "description": "Blocks in the section \u2014 any block except another section, grid, or walkthrough.",
          "items": {
            "discriminator": {
              "mapping": {
                "badge_row": "#/$defs/BadgeRow",
                "callout": "#/$defs/Callout",
                "cards": "#/$defs/Cards",
                "chart": "#/$defs/Chart",
                "code": "#/$defs/Code",
                "comparison": "#/$defs/Comparison",
                "def_list": "#/$defs/DefList",
                "fact_strip": "#/$defs/FactStrip",
                "fan": "#/$defs/Fan",
                "flow": "#/$defs/Flow",
                "heading": "#/$defs/Heading",
                "image": "#/$defs/Image",
                "key_value": "#/$defs/KeyValue",
                "list": "#/$defs/ListBlock",
                "meter": "#/$defs/Meter",
                "note": "#/$defs/Note",
                "quote": "#/$defs/Quote",
                "range": "#/$defs/Range",
                "references": "#/$defs/References",
                "status_list": "#/$defs/StatusList",
                "swimlane": "#/$defs/Swimlane",
                "table": "#/$defs/Table",
                "text": "#/$defs/Text",
                "timeline": "#/$defs/Timeline"
              },
              "propertyName": "type"
            },
            "oneOf": [
              {
                "$ref": "#/$defs/Heading"
              },
              {
                "$ref": "#/$defs/Text"
              },
              {
                "$ref": "#/$defs/ListBlock"
              },
              {
                "$ref": "#/$defs/FactStrip"
              },
              {
                "$ref": "#/$defs/KeyValue"
              },
              {
                "$ref": "#/$defs/DefList"
              },
              {
                "$ref": "#/$defs/Cards"
              },
              {
                "$ref": "#/$defs/BadgeRow"
              },
              {
                "$ref": "#/$defs/Callout"
              },
              {
                "$ref": "#/$defs/StatusList"
              },
              {
                "$ref": "#/$defs/Meter"
              },
              {
                "$ref": "#/$defs/Range"
              },
              {
                "$ref": "#/$defs/Table"
              },
              {
                "$ref": "#/$defs/Code"
              },
              {
                "$ref": "#/$defs/Quote"
              },
              {
                "$ref": "#/$defs/Note"
              },
              {
                "$ref": "#/$defs/Image"
              },
              {
                "$ref": "#/$defs/Timeline"
              },
              {
                "$ref": "#/$defs/Flow"
              },
              {
                "$ref": "#/$defs/Fan"
              },
              {
                "$ref": "#/$defs/Chart"
              },
              {
                "$ref": "#/$defs/Comparison"
              },
              {
                "$ref": "#/$defs/Swimlane"
              },
              {
                "$ref": "#/$defs/References"
              }
            ]
          },
          "minItems": 1,
          "title": "Blocks",
          "type": "array"
        }
      },
      "required": [
        "type",
        "title",
        "blocks"
      ],
      "title": "Section",
      "type": "object"
    },
    "StatusItem": {
      "additionalProperties": false,
      "properties": {
        "state": {
          "description": "Step state, driving the glyph: done, current (in progress), pending, failed, blocked.",
          "enum": [
            "done",
            "current",
            "pending",
            "failed",
            "blocked"
          ],
          "title": "State",
          "type": "string"
        },
        "text": {
          "description": "Rich-text label for the step.",
          "title": "Text",
          "type": "string"
        }
      },
      "required": [
        "state",
        "text"
      ],
      "title": "StatusItem",
      "type": "object"
    },
    "StatusList": {
      "additionalProperties": false,
      "properties": {
        "span": {
          "anyOf": [
            {
              "ge": 1,
              "le": 6,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Block width in content columns (1 to 6); omit for full width. Same column-count vocabulary as a grid cell's span, relative to the block's container (the page, or the enclosing grid cell when nested). Width only: blocks still stack vertically; use a `grid` to put several in one row.",
          "title": "Span"
        },
        "type": {
          "const": "status_list",
          "title": "Type",
          "type": "string"
        },
        "items": {
          "description": "Steps/checks with a coloured state glyph.",
          "items": {
            "$ref": "#/$defs/StatusItem"
          },
          "minItems": 1,
          "title": "Items",
          "type": "array"
        }
      },
      "required": [
        "type",
        "items"
      ],
      "title": "StatusList",
      "type": "object"
    },
    "Swimlane": {
      "additionalProperties": false,
      "properties": {
        "span": {
          "anyOf": [
            {
              "ge": 1,
              "le": 6,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Block width in content columns (1 to 6); omit for full width. Same column-count vocabulary as a grid cell's span, relative to the block's container (the page, or the enclosing grid cell when nested). Width only: blocks still stack vertically; use a `grid` to put several in one row.",
          "title": "Span"
        },
        "type": {
          "const": "swimlane",
          "title": "Type",
          "type": "string"
        },
        "lanes": {
          "description": "Lanes, in row order (top to bottom). A bare string is shorthand for `{name: \u2026}`; use `{id, name}` to give a stable reference key. Capped at 8 \u2014 more rows than that stop reading as a matrix; split into two swimlanes instead.",
          "items": {
            "$ref": "#/$defs/SwimlaneLane"
          },
          "maxItems": 8,
          "minItems": 1,
          "title": "Lanes",
          "type": "array"
        },
        "columns": {
          "description": "Columns, left to right \u2014 the sprint / phase axis. A bare string is shorthand for `{name: \u2026}`; use `{id, name, sub}` for a stable key and/or a secondary header caption.",
          "items": {
            "$ref": "#/$defs/SwimlaneColumn"
          },
          "minItems": 1,
          "title": "Columns",
          "type": "array"
        },
        "groups": {
          "description": "Optional milestone overlay: coloured caps beyond the table, each spanning a contiguous run of columns. Omit entirely for a plain swimlane.",
          "items": {
            "$ref": "#/$defs/SwimlaneGroup"
          },
          "title": "Groups",
          "type": "array"
        },
        "steps": {
          "description": "Steps placed on the lane/column grid; the sequence reads as a staircase.",
          "items": {
            "$ref": "#/$defs/SwimlaneStep"
          },
          "minItems": 1,
          "title": "Steps",
          "type": "array"
        }
      },
      "required": [
        "type",
        "lanes",
        "columns",
        "steps"
      ],
      "title": "Swimlane",
      "type": "object"
    },
    "SwimlaneColumn": {
      "additionalProperties": false,
      "properties": {
        "name": {
          "description": "Column header label.",
          "minLength": 1,
          "title": "Name",
          "type": "string"
        },
        "id": {
          "anyOf": [
            {
              "minLength": 1,
              "pattern": "^[A-Za-z0-9_-]+$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional stable id (ASCII letters, digits, _, -) that a step references via `col` and a group via `columns`; defaults to `name`. Set it to rename the header without touching every step/group.",
          "title": "Id"
        },
        "sub": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional secondary caption under the header (e.g. a delivery target or date range).",
          "title": "Sub"
        }
      },
      "required": [
        "name"
      ],
      "title": "SwimlaneColumn",
      "type": "object"
    },
    "SwimlaneGroup": {
      "additionalProperties": false,
      "properties": {
        "name": {
          "description": "Group (milestone / delivery) name, shown on its cap.",
          "minLength": 1,
          "title": "Name",
          "type": "string"
        },
        "color": {
          "description": "Cap colour \u2014 a palette name (slate/blue/\u2026) or its semantic tone twin (neutral/info/\u2026). Author-chosen, never auto-assigned: a group's colour carries meaning.",
          "enum": [
            "slate",
            "blue",
            "green",
            "amber",
            "red",
            "violet",
            "teal",
            "sky"
          ],
          "title": "Color",
          "type": "string"
        },
        "columns": {
          "description": "The columns this group spans, by their key (id, or name if no id) \u2014 a CONTIGUOUS run of the block's `columns` (a group cannot skip a column). Order need not match; it is derived from the block `columns`.",
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "title": "Columns",
          "type": "array"
        }
      },
      "required": [
        "name",
        "color",
        "columns"
      ],
      "title": "SwimlaneGroup",
      "type": "object"
    },
    "SwimlaneLane": {
      "additionalProperties": false,
      "properties": {
        "name": {
          "description": "Lane label shown in the row gutter.",
          "minLength": 1,
          "title": "Name",
          "type": "string"
        },
        "id": {
          "anyOf": [
            {
              "minLength": 1,
              "pattern": "^[A-Za-z0-9_-]+$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional stable id (ASCII letters, digits, _, -) that a step references via `lane`; defaults to `name`. Set it to rename the displayed label without touching every step.",
          "title": "Id"
        }
      },
      "required": [
        "name"
      ],
      "title": "SwimlaneLane",
      "type": "object"
    },
    "SwimlaneStep": {
      "additionalProperties": false,
      "properties": {
        "lane": {
          "description": "Which lane this step sits in \u2014 one of the block's `lanes`.",
          "minLength": 1,
          "title": "Lane",
          "type": "string"
        },
        "col": {
          "description": "Which column (sprint) this step sits in \u2014 one of the block's `columns`. Two steps sharing a lane/col stack in that cell.",
          "minLength": 1,
          "title": "Col",
          "type": "string"
        },
        "n": {
          "description": "The number shown in the step's cell \u2014 a free string ('1', '3a', 'R1'); skaldr never derives or renumbers it, so it reads exactly as written.",
          "minLength": 1,
          "title": "N",
          "type": "string"
        },
        "label": {
          "description": "Step label, shown beside the number.",
          "minLength": 1,
          "title": "Label",
          "type": "string"
        },
        "group": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Which group (milestone) this step belongs to \u2014 one of the block's `groups` that covers its `col`. Required only when the column is split across more than one group; inferred otherwise.",
          "title": "Group"
        },
        "value": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional numeric weight for this step (points, hours, cost, count \u2014 whatever the matrix measures). When any step in the block has a value, skaldr auto-sums them into per-column (footer row), per-lane (beside the lane label), and per-group (on the cap) totals \u2014 so the numbers never drift by hand. A step with no value counts as 0.",
          "title": "Value"
        },
        "url": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional link (http/https/mailto) for the step \u2014 e.g. its Jira/GitHub ticket. The step's number becomes a link out to it.",
          "title": "Url"
        },
        "state": {
          "default": "normal",
          "description": "Ticket emphasis. `normal` (default) is full-weight active work. `low` fades the ticket (solid, still clearly live) for low-priority / tail work. `blocked` marks it waiting / on-hold \u2014 dashed outline + a greyed number badge, distinct from `low`. The value counts toward the totals in every state.",
          "enum": [
            "normal",
            "low",
            "blocked"
          ],
          "title": "State",
          "type": "string"
        },
        "id": {
          "anyOf": [
            {
              "minLength": 1,
              "pattern": "^[A-Za-z0-9_-]+$",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional stable id (ASCII letters, digits, _, -) so other steps can point at this one via `depends_on`. Unique across the block's steps.",
          "title": "Id"
        },
        "depends_on": {
          "description": "Ids of steps this one depends on. The dependent renders a compact 'needs N' marker listing each referenced step's number. Each id must be a step's `id`; a step can't depend on itself.",
          "items": {
            "type": "string"
          },
          "title": "Depends On",
          "type": "array"
        }
      },
      "required": [
        "lane",
        "col",
        "n",
        "label"
      ],
      "title": "SwimlaneStep",
      "type": "object"
    },
    "Table": {
      "additionalProperties": false,
      "properties": {
        "span": {
          "anyOf": [
            {
              "ge": 1,
              "le": 6,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Block width in content columns (1 to 6); omit for full width. Same column-count vocabulary as a grid cell's span, relative to the block's container (the page, or the enclosing grid cell when nested). Width only: blocks still stack vertically; use a `grid` to put several in one row.",
          "title": "Span"
        },
        "type": {
          "const": "table",
          "title": "Type",
          "type": "string"
        },
        "columns": {
          "description": "Column specs; at least one text/rich column.",
          "items": {
            "$ref": "#/$defs/Column"
          },
          "minItems": 1,
          "title": "Columns",
          "type": "array"
        },
        "groups": {
          "anyOf": [
            {
              "items": {
                "$ref": "#/$defs/Group"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Grouped rows with derived subtotals; provide this OR `rows`, not both.",
          "title": "Groups"
        },
        "rows": {
          "anyOf": [
            {
              "items": {
                "anyOf": [
                  {
                    "additionalProperties": true,
                    "type": "object"
                  },
                  {
                    "items": {},
                    "type": "array"
                  }
                ]
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Ungrouped rows; provide this OR `groups`, not both. Each row is a mapping (column key \u2192 value) or a positional list in the declared column order.",
          "title": "Rows"
        },
        "reconcile": {
          "anyOf": [
            {
              "$ref": "#/$defs/Reconcile"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Opt-in trust check on a number column."
        },
        "totals": {
          "anyOf": [
            {
              "$ref": "#/$defs/Totals"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Sum a number column into a footer row."
        },
        "rollup": {
          "anyOf": [
            {
              "$ref": "#/$defs/Rollup"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Opt-in summary strip below the table: counts the rows by a badge column and shows one '<chip> <count>' per value, derived from the rows so it can never drift from them."
        },
        "tint_by": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Opt-in row heatmap: faintly tint each row by the tone of the badge in this column (named by its key), so a long table's state reads as bands of colour. A row left blank in that column stays untinted; an explicit row `tone` (muted/danger) wins over the tint. Must name a `badge` column (the same one may still render its chip).",
          "title": "Tint By"
        }
      },
      "required": [
        "type",
        "columns"
      ],
      "title": "Table",
      "type": "object"
    },
    "Text": {
      "additionalProperties": false,
      "properties": {
        "span": {
          "anyOf": [
            {
              "ge": 1,
              "le": 6,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Block width in content columns (1 to 6); omit for full width. Same column-count vocabulary as a grid cell's span, relative to the block's container (the page, or the enclosing grid cell when nested). Width only: blocks still stack vertically; use a `grid` to put several in one row.",
          "title": "Span"
        },
        "type": {
          "const": "text",
          "title": "Type",
          "type": "string"
        },
        "body": {
          "description": "Rich-text prose; blank lines split paragraphs.",
          "title": "Body",
          "type": "string"
        },
        "muted": {
          "default": false,
          "description": "Render in the caption colour, for asides.",
          "title": "Muted",
          "type": "boolean"
        }
      },
      "required": [
        "type",
        "body"
      ],
      "title": "Text",
      "type": "object"
    },
    "Timeline": {
      "additionalProperties": false,
      "properties": {
        "span": {
          "anyOf": [
            {
              "ge": 1,
              "le": 6,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Block width in content columns (1 to 6); omit for full width. Same column-count vocabulary as a grid cell's span, relative to the block's container (the page, or the enclosing grid cell when nested). Width only: blocks still stack vertically; use a `grid` to put several in one row.",
          "title": "Span"
        },
        "type": {
          "const": "timeline",
          "title": "Type",
          "type": "string"
        },
        "items": {
          "description": "Ordered entries with state-coloured dots.",
          "items": {
            "$ref": "#/$defs/TimelineItem"
          },
          "minItems": 1,
          "title": "Items",
          "type": "array"
        }
      },
      "required": [
        "type",
        "items"
      ],
      "title": "Timeline",
      "type": "object"
    },
    "TimelineItem": {
      "additionalProperties": false,
      "properties": {
        "time": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional timestamp/label for the entry.",
          "title": "Time"
        },
        "title": {
          "description": "Entry title.",
          "title": "Title",
          "type": "string"
        },
        "body": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional rich-text detail.",
          "title": "Body"
        },
        "state": {
          "anyOf": [
            {
              "enum": [
                "done",
                "current",
                "pending"
              ],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional dot state: done, current, pending.",
          "title": "State"
        },
        "badges": {
          "description": "Declared badge keys (from the page `badges`) to chip onto this entry.",
          "items": {
            "type": "string"
          },
          "title": "Badges",
          "type": "array"
        }
      },
      "required": [
        "title"
      ],
      "title": "TimelineItem",
      "type": "object"
    },
    "Totals": {
      "additionalProperties": false,
      "properties": {
        "column": {
          "description": "Key of the number column to sum into a footer row.",
          "title": "Column",
          "type": "string"
        }
      },
      "required": [
        "column"
      ],
      "title": "Totals",
      "type": "object"
    },
    "Walkthrough": {
      "additionalProperties": false,
      "properties": {
        "span": {
          "anyOf": [
            {
              "ge": 1,
              "le": 6,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Block width in content columns (1 to 6); omit for full width. Same column-count vocabulary as a grid cell's span, relative to the block's container (the page, or the enclosing grid cell when nested). Width only: blocks still stack vertically; use a `grid` to put several in one row.",
          "title": "Span"
        },
        "type": {
          "const": "walkthrough",
          "title": "Type",
          "type": "string"
        },
        "steps": {
          "description": "Ordered steps; each is a big numbered title beside its detail column.",
          "items": {
            "$ref": "#/$defs/WalkthroughStep"
          },
          "minItems": 1,
          "title": "Steps",
          "type": "array"
        },
        "step_span": {
          "default": 2,
          "description": "Width of the title column, of 6; the detail column takes the rest (default 2).",
          "maximum": 5,
          "minimum": 1,
          "title": "Step Span",
          "type": "integer"
        }
      },
      "required": [
        "type",
        "steps"
      ],
      "title": "Walkthrough",
      "type": "object"
    },
    "WalkthroughStep": {
      "additionalProperties": false,
      "properties": {
        "label": {
          "description": "Step title \u2014 a few words to a short sentence; it wraps across lines, so it can be long.",
          "minLength": 1,
          "title": "Label",
          "type": "string"
        },
        "sub": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional one-line sub-label under the title (rich text).",
          "title": "Sub"
        },
        "tone": {
          "anyOf": [
            {
              "enum": [
                "neutral",
                "info",
                "success",
                "warning",
                "danger",
                "accent",
                "teal",
                "sky"
              ],
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional tone; draws a subtle accent down the step's inline-start edge (the numeral itself is always a uniform muted grey, so tone can't leave steps looking mismatched).",
          "title": "Tone"
        },
        "detail": {
          "description": "The step's detail (paragraphs, lists, code, callouts, tables, or a `grid` for a two-column step like Action | Script), rendered in the column beside the numbered title.",
          "items": {
            "discriminator": {
              "mapping": {
                "badge_row": "#/$defs/BadgeRow",
                "callout": "#/$defs/Callout",
                "cards": "#/$defs/Cards",
                "chart": "#/$defs/Chart",
                "code": "#/$defs/Code",
                "comparison": "#/$defs/Comparison",
                "def_list": "#/$defs/DefList",
                "fact_strip": "#/$defs/FactStrip",
                "fan": "#/$defs/Fan",
                "flow": "#/$defs/Flow",
                "grid": "#/$defs/InnerGrid",
                "heading": "#/$defs/Heading",
                "image": "#/$defs/Image",
                "key_value": "#/$defs/KeyValue",
                "list": "#/$defs/ListBlock",
                "meter": "#/$defs/Meter",
                "note": "#/$defs/Note",
                "quote": "#/$defs/Quote",
                "range": "#/$defs/Range",
                "references": "#/$defs/References",
                "status_list": "#/$defs/StatusList",
                "swimlane": "#/$defs/Swimlane",
                "table": "#/$defs/Table",
                "text": "#/$defs/Text",
                "timeline": "#/$defs/Timeline"
              },
              "propertyName": "type"
            },
            "oneOf": [
              {
                "$ref": "#/$defs/Heading"
              },
              {
                "$ref": "#/$defs/Text"
              },
              {
                "$ref": "#/$defs/ListBlock"
              },
              {
                "$ref": "#/$defs/FactStrip"
              },
              {
                "$ref": "#/$defs/KeyValue"
              },
              {
                "$ref": "#/$defs/DefList"
              },
              {
                "$ref": "#/$defs/Cards"
              },
              {
                "$ref": "#/$defs/BadgeRow"
              },
              {
                "$ref": "#/$defs/Callout"
              },
              {
                "$ref": "#/$defs/StatusList"
              },
              {
                "$ref": "#/$defs/Meter"
              },
              {
                "$ref": "#/$defs/Range"
              },
              {
                "$ref": "#/$defs/Table"
              },
              {
                "$ref": "#/$defs/Code"
              },
              {
                "$ref": "#/$defs/Quote"
              },
              {
                "$ref": "#/$defs/Note"
              },
              {
                "$ref": "#/$defs/Image"
              },
              {
                "$ref": "#/$defs/Timeline"
              },
              {
                "$ref": "#/$defs/Flow"
              },
              {
                "$ref": "#/$defs/Fan"
              },
              {
                "$ref": "#/$defs/Chart"
              },
              {
                "$ref": "#/$defs/Comparison"
              },
              {
                "$ref": "#/$defs/Swimlane"
              },
              {
                "$ref": "#/$defs/References"
              },
              {
                "$ref": "#/$defs/InnerGrid"
              }
            ]
          },
          "minItems": 1,
          "title": "Detail",
          "type": "array"
        }
      },
      "required": [
        "label",
        "detail"
      ],
      "title": "WalkthroughStep",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "properties": {
    "version": {
      "const": 1,
      "description": "Content-file schema version.",
      "title": "Version",
      "type": "integer"
    },
    "meta": {
      "$ref": "#/$defs/Meta"
    },
    "badges": {
      "additionalProperties": {
        "$ref": "#/$defs/Badge"
      },
      "description": "Author-declared tag/status vocabulary.",
      "title": "Badges",
      "type": "object"
    },
    "blocks": {
      "items": {
        "discriminator": {
          "mapping": {
            "badge_row": "#/$defs/BadgeRow",
            "callout": "#/$defs/Callout",
            "cards": "#/$defs/Cards",
            "chart": "#/$defs/Chart",
            "code": "#/$defs/Code",
            "comparison": "#/$defs/Comparison",
            "def_list": "#/$defs/DefList",
            "fact_strip": "#/$defs/FactStrip",
            "fan": "#/$defs/Fan",
            "flow": "#/$defs/Flow",
            "grid": "#/$defs/Grid",
            "heading": "#/$defs/Heading",
            "image": "#/$defs/Image",
            "key_value": "#/$defs/KeyValue",
            "list": "#/$defs/ListBlock",
            "meter": "#/$defs/Meter",
            "note": "#/$defs/Note",
            "panel": "#/$defs/Panel",
            "quote": "#/$defs/Quote",
            "range": "#/$defs/Range",
            "references": "#/$defs/References",
            "section": "#/$defs/Section",
            "status_list": "#/$defs/StatusList",
            "swimlane": "#/$defs/Swimlane",
            "table": "#/$defs/Table",
            "text": "#/$defs/Text",
            "timeline": "#/$defs/Timeline",
            "walkthrough": "#/$defs/Walkthrough"
          },
          "propertyName": "type"
        },
        "oneOf": [
          {
            "$ref": "#/$defs/Heading"
          },
          {
            "$ref": "#/$defs/Text"
          },
          {
            "$ref": "#/$defs/ListBlock"
          },
          {
            "$ref": "#/$defs/FactStrip"
          },
          {
            "$ref": "#/$defs/KeyValue"
          },
          {
            "$ref": "#/$defs/DefList"
          },
          {
            "$ref": "#/$defs/Cards"
          },
          {
            "$ref": "#/$defs/BadgeRow"
          },
          {
            "$ref": "#/$defs/Callout"
          },
          {
            "$ref": "#/$defs/StatusList"
          },
          {
            "$ref": "#/$defs/Meter"
          },
          {
            "$ref": "#/$defs/Range"
          },
          {
            "$ref": "#/$defs/Table"
          },
          {
            "$ref": "#/$defs/Code"
          },
          {
            "$ref": "#/$defs/Quote"
          },
          {
            "$ref": "#/$defs/Note"
          },
          {
            "$ref": "#/$defs/Image"
          },
          {
            "$ref": "#/$defs/Timeline"
          },
          {
            "$ref": "#/$defs/Flow"
          },
          {
            "$ref": "#/$defs/Fan"
          },
          {
            "$ref": "#/$defs/Chart"
          },
          {
            "$ref": "#/$defs/Comparison"
          },
          {
            "$ref": "#/$defs/Swimlane"
          },
          {
            "$ref": "#/$defs/References"
          },
          {
            "$ref": "#/$defs/Section"
          },
          {
            "$ref": "#/$defs/Grid"
          },
          {
            "$ref": "#/$defs/Walkthrough"
          },
          {
            "$ref": "#/$defs/Panel"
          }
        ]
      },
      "minItems": 1,
      "title": "Blocks",
      "type": "array"
    }
  },
  "required": [
    "version",
    "meta",
    "blocks"
  ],
  "title": "Report",
  "type": "object"
}
